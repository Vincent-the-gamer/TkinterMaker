const conf = require('../package.json')
export const preview = {
    url: 'http://127.0.0.1:6050',
    version: conf.version,
}

export default {
    name: 'Tkinter页面制作小工具',
    version: conf.version,
}